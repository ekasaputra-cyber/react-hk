'use client'

import { useState } from 'react';

const NamOrt = () => {
  const [showDetail, setShowDetail] = useState(false);

  return (
    <div className="p-4">
            <h1 className="text-xl font-bold">Hai yaya</h1>
    </div>
  );
};

export default NamOrt;